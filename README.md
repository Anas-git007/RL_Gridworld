# RL_Gridworld
A tabular reinforcement learning project that compares **Q-learning (ε-greedy)** and **Softmax (Boltzmann) policies** in a stochastic Gridworld environment. The agent learns to navigate from start to goal while avoiding traps, with experiments analyzing how exploration strategies behave under different levels of transition noise and uncertainty.
